info:
  name: Obter Histórico de Pagamentos (Paginado)
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}payments"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
