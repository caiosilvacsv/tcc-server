info:
  name: Listar Produtos (Catálogo)
  type: http
  seq: 8

http:
  method: GET
  url: "{{localhost}}product"
  body:
    type: none
  auth: none

query:
  activeOnly: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
