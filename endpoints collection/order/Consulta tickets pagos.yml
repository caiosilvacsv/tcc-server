info:
  name: Consulta tickets pagos
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}orders/tickets"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
