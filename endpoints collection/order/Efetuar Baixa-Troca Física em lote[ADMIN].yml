info:
  name: Efetuar Baixa/Troca Física em lote[ADMIN]
  type: http
  seq: 7

http:
  method: PUT
  url: "{{host}}orders/items/exchange"
  body:
    type: json
    data: |-
      {
        "itemIds":[
          ""
        ]
      }
  auth:
    type: bearer
    token: "{{tokenADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
