info:
  name: Efetuar consulta do item especifico de um pedido [auth]
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}orders/items/:idItem"
  params:
    - name: idItem
      value: aa7463cb-a27a-420d-9aad-258165fdec54
      type: path
  auth:
    type: bearer
    token: "{{tokenADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
