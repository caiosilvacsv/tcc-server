info:
  name: Historico de pedidos do usuário
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}orders"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
