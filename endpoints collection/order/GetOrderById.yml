info:
  name: Obter Pedido por ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{host}}orders/:orderID"
  params:
    - name: orderID
      value: f8b3a1e5-6d03-49fb-ba38-f88a23c2502b
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
