info:
  name: CreateOrder
  type: http
  seq: 3

http:
  method: POST
  url: "{{localhost}}api/orders"
  body:
    type: json
    data: |-
      {
        "itemsList": [
          {
            "productId": "insira-id-do-produto-aqui",
            "quantity": 3
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
