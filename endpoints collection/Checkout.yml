info:
  name: Checkout
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}payment/checkout"
  body:
    type: json
    data: |-
      {
        "orderId": "insira-id-do-pedido-aqui"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
