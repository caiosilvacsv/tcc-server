info:
  name: Checkout Simplificado (Anônimo)
  type: http
  seq: 4

http:
  method: POST
  url: "{{localhost}}api/payments/anonymous"
  body:
    type: json
    data: |-
      {
        "productID": "coloque-o-uuid-do-produto-aqui",
        "quantity": 2,
        "contributorCpfCnpj": "12345678909",
        "contributorName": "Nome do Contribuinte Anônimo"
      }
  auth: none

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
