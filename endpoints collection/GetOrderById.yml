info:
  name: Obter Pedido por ID
  type: http
  seq: 6

http:
  method: GET
  url: "{{localhost}}api/orders/insira-id-do-pedido-aqui"
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
