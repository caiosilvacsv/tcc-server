info:
  name: Listar Pedidos do Aluno
  type: http
  seq: 5

http:
  method: GET
  url: "{{localhost}}api/orders"
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
