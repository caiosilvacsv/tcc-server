info:
  name: Obter Perfil do Usuário Logado (Me)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}auth/me"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
