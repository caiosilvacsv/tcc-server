info:
  name: Atualizar Produto (ADMIN)
  type: http
  seq: 7

http:
  method: PUT
  url: "{{host}}product/:id"
  params:
    - name: id
      value: 4d995301-c58e-427e-87e6-bf52b31fd3d8
      type: path
  body:
    type: json
    data: |-
      {
        "title": "Ticket interno",
        "description": "Ticket exclusivo para pessoas bonitas.",
        "image": "ticketBonita.png",
        "price": 0.100,
        "active": true,
        "codeService": "0245",
        "category": "TICKET"
      }
  auth:
    type: bearer
    token: "{{tokenADMIN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
