info:
  name: Listar categorias (Catálogo)
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}product/categories"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
