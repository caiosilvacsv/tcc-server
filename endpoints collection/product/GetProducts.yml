info:
  name: Listar Produtos (Catálogo)
  type: http
  seq: 4

http:
  method: GET
  url: "{{host}}product"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
