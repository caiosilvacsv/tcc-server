info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{localhost}}auth/register"
  body:
    type: json
    data: |-
      {
        "login" : "caio",
        "password" : "1234",
        "role":"ADMIN"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
